# 节点管理模块

## TC-NODE-001: 注册节点

**测试目标**: 验证节点注册功能

**请求**
```bash
POST /api/v1/msp/nodes/register
Content-Type: application/json
Authorization: Bearer <token>

{
  "nodeId": "node1",
  "nodeName": "测试节点1",
  "endpoint": "http://node1:8080",
  "capabilities": ["PSI", "MPC"],
  "tags": ["测试"]
}
```

**预期响应**
```json
{
  "success": true,
  "code": "200",
  "message": "success",
  "data": {
    "nodeId": "<节点ID>",
    "status": "ONLINE"
  }
}
```

**执行结果**: ✅ 通过

---

## TC-NODE-002: 查询节点列表

**测试目标**: 验证节点列表查询

**请求**
```bash
GET /api/v1/msp/nodes?page=0&size=10
Authorization: Bearer <token>
```

**预期响应**
```json
{
  "success": true,
  "code": "200",
  "message": "success",
  "data": {
    "content": [
      {
        "nodeId": "<节点ID>",
        "nodeName": "测试节点1",
        "status": "ONLINE",
        "endpoint": "http://node1:8080",
        "capabilities": ["PSI", "MPC"],
        "tags": ["测试"],
        "createTime": 1747891200000
      }
    ],
    "total": 1,
    "page": 0,
    "size": 10
  }
}
```

**执行结果**: ✅ 通过 (2026-05-25)

---

## TC-NODE-003: 查询节点详情

**测试目标**: 验证节点详情查询

**请求**
```bash
GET /api/v1/msp/nodes/<nodeId>
Authorization: Bearer <token>
```

**预期响应**
```json
{
  "success": true,
  "code": "200",
  "message": "success",
  "data": {
    "nodeId": "<节点ID>",
    "nodeName": "测试节点1",
    "status": "ONLINE",
    "endpoint": "http://node1:8080",
    "capabilities": ["PSI", "MPC"],
    "tags": ["测试"],
    "createTime": 1747891200000,
    "updateTime": 1747891200000
  }
}
```

**执行结果**: ⚠️ 待正式节点注册后测试 (模拟节点暂不支持详情查询)

---

## TC-NODE-004: 节点心跳

**测试目标**: 验证节点心跳上报功能

**请求**
```bash
POST /api/v1/msp/nodes/<nodeId>/heartbeat
Authorization: Bearer <token>
```

**预期响应**
```json
{
  "success": true,
  "code": "200",
  "message": "success",
  "data": true
}
```

**执行结果**: ✅ 通过 (2026-05-25)

---

## TC-NODE-005: 注销节点

**测试目标**: 验证节点注销功能

**请求**
```bash
DELETE /api/v1/msp/nodes/<nodeId>
Authorization: Bearer <token>
```

**预期响应**
```json
{
  "success": true,
  "code": "200",
  "message": "success",
  "data": true
}
```

**执行结果**: ⚠️ 待正式节点注册后测试

---

## UI 自动化测试 (Playwright CLI)

**测试时间**: 2026-05-29
**runId**: jnpf-login-20260529-153622
**URL**: http://localhost:3000/nodes

### 用例执行结果

| 用例 ID | 用例名称 | 状态 | 说明 |
|---------|---------|------|------|
| TC001 | 登录成功 | ✅ 通过 | 登录后跳转至 dashboard |
| TC002 | 导航到节点管理 | ✅ 通过 | 直接访问 /nodes |
| TC003 | 刷新功能 | ✅ 通过 | 点击刷新后页面重新加载 |
| TC004 | 注册节点-打开表单 | ✅ 通过 | 点击按钮弹出注册节点对话框 |
| TC005 | 注册节点-必填校验 | ✅ 通过 | 空提交触发校验提示 |
| TC006 | 注册节点-正常填写 | ✅ 通过 | 表单字段可正常填写 |
| TC007 | 注册节点-取消关闭 | ✅ 通过 | 取消后关闭弹窗，返回列表页 |
| TC008 | 注销节点 | ✅ 通过 | 注销时有确认弹窗，可取消 |

### 发现的问题

1. **Console 错误**: 节点管理页面存在持续 console error，可能与后端 API 相关

### 界面元素 ref 一览

| 元素 | Ref |
|------|-----|
| 刷新按钮 | e74 |
| 注册节点按钮 | e79 |
| 节点ID输入框 | e198 |
| 节点名称输入框 | e204 |
| 端点输入框 | e210 |
| PYU能力复选框 | e216 |
| SPU能力复选框 | e220 |
| HEU能力复选框 | e224 |
| TEEU能力复选框 | e228 |
| 标签输入框 | e236 |
| 取消按钮 | e238 |
| 注册按钮 | e240 |
| 注销按钮 | e136/e150/e164 |

### 产出物

- 测试计划: `docs/playwright-cli-plans/node-management.json`
- 测试报告: `artifacts/playwright-cli/runs/jnpf-login-20260529-153622/run-report.md`
- 快照目录: `artifacts/playwright-cli/runs/jnpf-login-20260529-153622/snapshots/`

---

## 汇总

| 用例ID | 名称 | 优先级 | 执行结果 |
|--------|------|--------|----------|
| TC-NODE-001 | 注册节点 | P0 | ✅ 通过 |
| TC-NODE-002 | 查询节点列表 | P0 | ✅ 通过 (2026-05-25) |
| TC-NODE-003 | 查询节点详情 | P0 | ⚠️ 待正式节点 |
| TC-NODE-004 | 节点心跳 | P0 | ✅ 通过 (2026-05-25) |
| TC-NODE-005 | 注销节点 | P0 | ⚠️ 待正式节点 |
