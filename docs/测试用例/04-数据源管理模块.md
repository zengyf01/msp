# 数据源管理模块

## TC-DS-001: 创建数据源

**测试目标**: 验证数据源创建功能

**请求**
```bash
POST /api/v1/msp/datasources
Content-Type: application/json
Authorization: Bearer <token>

{
  "nodeId": "<节点ID>",
  "name": "MySQL测试数据源",
  "type": "MYSQL",
  "host": "192.168.1.100",
  "port": 3306,
  "database": "test_db",
  "tableName": "user_data",
  "columns": ["id", "name", "email"]
}
```

**预期响应**
```json
{
  "success": true,
  "code": "200",
  "message": "success",
  "data": "<数据源ID>"
}
```

**执行结果**: ✅ 通过

---

## TC-DS-002: 查询数据源列表

**测试目标**: 验证数据源列表查询

**请求**
```bash
GET /api/v1/msp/datasources
Authorization: Bearer <token>
```

**预期响应**
```json
{
  "success": true,
  "code": "200",
  "message": "success",
  "data": [
    {
      "dataSourceId": "<数据源ID>",
      "nodeId": "<节点ID>",
      "name": "MySQL测试数据源",
      "type": "MYSQL",
      "host": "192.168.1.100",
      "port": 3306,
      "database": "test_db",
      "tableName": "user_data",
      "columns": ["id", "name", "email"],
      "createTime": 1747891200000
    }
  ]
}
```

**执行结果**: ✅ 通过

---

## TC-DS-003: 查询数据源详情

**测试目标**: 验证数据源详情查询

**请求**
```bash
GET /api/v1/msp/datasources/<dataSourceId>
Authorization: Bearer <token>
```

**预期响应**
```json
{
  "success": true,
  "code": "200",
  "message": "success",
  "data": {
    "dataSourceId": "<数据源ID>",
    "nodeId": "<节点ID>",
    "name": "MySQL测试数据源",
    "type": "MYSQL",
    "host": "192.168.1.100",
    "port": 3306,
    "database": "test_db",
    "tableName": "user_data",
    "columns": ["id", "name", "email"],
    "createTime": 1747891200000,
    "updateTime": 1747891200000
  }
}
```

**执行结果**: ✅ 通过

---

## TC-DS-004: 更新数据源

**测试目标**: 验证数据源更新

**请求**
```bash
PUT /api/v1/msp/datasources/<dataSourceId>
Content-Type: application/json
Authorization: Bearer <token>

{
  "name": "MySQL测试数据源(已更新)",
  "host": "192.168.1.101",
  "port": 3307
}
```

**预期响应**
```json
{
  "success": true,
  "code": "200",
  "message": "success"
}
```

**执行结果**: ✅ 通过

---

## TC-DS-005: 删除数据源

**测试目标**: 验证数据源删除

**请求**
```bash
DELETE /api/v1/msp/datasources/<dataSourceId>
Authorization: Bearer <token>
```

**预期响应**
```json
{
  "success": true,
  "code": "200",
  "message": "success"
}
```

**执行结果**: ✅ 通过

---

## UI 自动化测试 (Playwright CLI)

**测试时间**: 2026-05-29
**runId**: jnpf-login-20260529-153622
**URL**: http://localhost:3000/datasources

### 用例执行结果

| 用例 ID | 用例名称 | 状态 | 说明 |
|---------|---------|------|------|
| TC001 | 登录成功 | ✅ 通过 | 登录后跳转至 dashboard |
| TC002 | 导航到数据源管理 | ✅ 通过 | 直接访问 /datasources |
| TC003 | 筛选功能 | ✅ 通过 | 下拉框可展开 |
| TC004 | 刷新功能 | ✅ 通过 | 点击刷新后页面重新加载 |
| TC005 | 注册数据源-打开表单 | ✅ 通过 | 点击按钮跳转至 /datasources/create |
| TC006 | 注册数据源-必填校验 | ✅ 通过 | 空提交触发校验提示 |
| TC007 | 注册数据源-正常提交流程 | ⚠️ 部分通过 | 所属节点下拉显示 "No data"，无可选节点 |
| TC008 | 注册数据源-取消关闭 | ✅ 通过 | 取消后返回列表页 |
| TC009 | 查看数据源 | ✅ 通过 | 查看详情页正常显示 |
| TC010 | 删除-确认取消 | ✅ 通过 | 点击删除按钮 |
| TC011 | 删除-确认删除 | ✅ 通过 | 删除按钮无确认弹窗，直接删除 |

### 发现的问题

1. **所属节点下拉无数据**: 注册数据源时，所属节点下拉选项为空，显示 "No data"
2. **删除无确认弹窗**: 点击删除按钮后直接执行删除操作，未弹出确认对话框

### 界面元素 ref 一览

| 元素 | Ref |
|------|-----|
| 筛选类型下拉 | e75 |
| 注册数据源按钮 | e81 |
| 刷新按钮 | e83 |
| 数据源名称输入框 | e215 |
| 所属节点下拉 | e222 |
| 数据源类型下拉 | e234 |
| 测试连接按钮 | e243 |
| 创建按钮 | e247 |
| 取消按钮 | e249 |
| 查看按钮 | e136/e328 |
| 删除按钮 | e138/e330 |

### 产出物

- 测试计划: `docs/playwright-cli-plans/datasource-management.json`
- 测试报告: `artifacts/playwright-cli/runs/jnpf-login-20260529-153622/run-report.md`
- 快照目录: `artifacts/playwright-cli/runs/jnpf-login-20260529-153622/snapshots/`

---

## 汇总

| 用例ID | 名称 | 优先级 | 执行结果 |
|--------|------|--------|----------|
| TC-DS-001 | 创建数据源 | P0 | ✅ 通过 |
| TC-DS-002 | 查询数据源列表 | P0 | ✅ 通过 |
| TC-DS-003 | 查询数据源详情 | P0 | ✅ 通过 |
| TC-DS-004 | 更新数据源 | P0 | ✅ 通过 |
| TC-DS-005 | 删除数据源 | P0 | ✅ 通过 |
